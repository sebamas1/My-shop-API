CREATE TABLE Usuario (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR(255) NOT NULL,
    EMAIL VARCHAR(255) NOT NULL
);
INSERT INTO Usuario (ID ,NAME, EMAIL) VALUES (1, 'Seeba', 'queseyo');